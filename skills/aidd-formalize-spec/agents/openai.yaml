interface:
  display_name: "AIDD Formalize Spec"
  short_description: "Turn natural language into reviewable formal specifications"
  default_prompt: "Use $aidd-formalize-spec to structure these requirements as a candidate formal specification and verify them."
